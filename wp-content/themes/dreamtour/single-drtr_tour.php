<?php
/**
 * Single template for DRTR tours (CPT `drtr_tour`).
 * Reuses the main single-tour template markup.
 *
 * @package DreamTour
 */

// Reuse the existing single-tour template for DRTR tours to keep layouts in sync.
locate_template('single-tour.php', true);
