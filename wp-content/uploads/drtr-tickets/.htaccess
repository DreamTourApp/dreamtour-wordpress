# Allow access to QR code images
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow direct access to PNG files
<FilesMatch "\.(png|jpg|jpeg|gif)$">
    Order allow,deny
    Allow from all
    Require all granted
</FilesMatch>

# Prevent directory listing
Options -Indexes
