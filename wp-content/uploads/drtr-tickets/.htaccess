# Allow access to QR code images and ticket files
<IfModule mod_rewrite.c>
    RewriteEngine Off
</IfModule>

# Allow direct access to all files
<FilesMatch ".*">
    Order allow,deny
    Allow from all
    Require all granted
</FilesMatch>

# Prevent directory listing
Options -Indexes

# Allow from all
Satisfy Any
